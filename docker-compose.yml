services:
  ip-intel:
    build: .
    ports:
      - "9000:8000"
    env_file:
      - .env
    environment:
      IP_INTEL_DB_PATH: /app/data/ip_intel.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped

services:
  ip-intel:
    build: .
    ports:
      - "9000:9000"
    env_file:
      - .env
    environment:
      IP_INTEL_DB_PATH: /data/ip_intel.db
    volumes:
      - ./data:/data
    restart: unless-stopped
